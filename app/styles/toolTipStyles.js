var CIRCLE_SIZE = 30;
var STICK_SIZE = 10; 
var STICK_WIDTH = 3;

var generalStyle = { 
  position: 'absolute',
  width: CIRCLE_SIZE,
  height: CIRCLE_SIZE + STICK_SIZE,
  left: -CIRCLE_SIZE / 2,
  top: -(CIRCLE_SIZE + STICK_SIZE)
}; 

